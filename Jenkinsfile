pipeline {
    agent any
    stages {
        stage('git-clone') {
            steps {
                sh 'https://github.com/srikar245/StudentCoursesRestAPI.git'
                // Change file permisson
                sh "chmod +x -R ./jenkins"
                              }
                           }
           }
}
#test
